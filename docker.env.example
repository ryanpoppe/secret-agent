# Code Camp 2025 Docker Environment Configuration
# Copy this file to .env before running docker-compose

# ===========================================
# Database Configuration
# ===========================================
POSTGRES_USER=codecamp
POSTGRES_PASSWORD=codecamp_secret_change_me
POSTGRES_DB=codecamp

# ===========================================
# Backend API Configuration
# ===========================================

# Optional API key for lead submission endpoint
# If set, clients must include "Authorization: Bearer <key>" header
API_KEY=

# CORS allowed origins (comma-separated for multiple)
CORS_ORIGIN=http://localhost

# ===========================================
# Frontend Configuration
# ===========================================

# API endpoint for lead submissions (proxied through nginx)
VITE_API_ENDPOINT=/api/leads

# Optional: Direct API key if frontend needs to authenticate
# VITE_API_KEY=

